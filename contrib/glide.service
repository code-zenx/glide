[Unit]
Description=glide — shared keyboard and mouse
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=%h/projects/glide/target/release/glide run
Restart=always
RestartSec=2
Environment=RUST_LOG=glide=info

[Install]
WantedBy=graphical-session.target
